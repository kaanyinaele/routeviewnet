#!/bin/sh
# prerm — stop and disable the service before the binary is removed.
set -e

if [ "$1" = "remove" ] || [ "$1" = "purge" ]; then
  systemctl stop routeviewnetd >/dev/null 2>&1 || true
  systemctl disable routeviewnetd >/dev/null 2>&1 || true
fi

exit 0
